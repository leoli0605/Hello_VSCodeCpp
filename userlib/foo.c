#include "foo.h"

#include <stdio.h>

// Function to add two integers
int add(int a, int b) {
    return a + b;
}

// Function to print a message
void print_message(const char *message) {
    printf("%s\n", message);
}
